package cli

func Run(appName string, version string, args []string, baseCommand Command) error {
	//var err, commandPath = getCommandPath(args)
	// getCommandPath from args
	// getMatching command
	// if no match return error

	// get normalized flags
	// process flags using app and command flag options
	// if error return error

	// execute command action
	// if error return error
	return nil
}

func getCommandPath(args []string) ([]string, error) {
	return nil, nil
}

func getFlags(args []string) map[string]string {
	return nil
}

func processFlags(flags map[string]string) error {
	return nil
}
